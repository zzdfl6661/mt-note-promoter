# 美团经营宝笔记推广自动化 — 项目状态报告

> 更新日期：2026-08-07 16:20
> 状态：**批量人群任务完成 + 主流程已内置定向人群**（等待下一步指令）

---

## 〇、最新进展（2026-08-07）

### 批量修改推广人群（智选 → 定向 + 自定义标签 9 项）

- 工具：`scripts/batch_target_audience.py`（独立脚本，复用 CDP 登录态，进度断点续跑）
- 范围：智选展位推广列表 **3 页 × 100条/页 = 300 条**，已全部扫描
- 结果：**已处理 306 条**（成功提交确认的推广占绝大多数，含已定向自动跳过）
- 最终失败 **9 条**（兴趣标签懒加载未渲染，5/9 保护跳过提交）→ 已在 `手动处理清单.md` 列明，**用户已人工修改完成**
- 关键流程（已验证）：「保存设置」真实点击 + 确认抽屉关闭 →「保存并提交」→ 右侧弹窗点「继续提交」→ 以「提交成功」弹窗为成功标志

### 主流程内置定向人群（flow.py）

- `_do_post_store_steps` 顺序改为：`_set_region`（地域6km）→ **`_set_crowd_targeting`（定向人群，新增）** → `_set_bid`（出价1.1）→ 下一步 → 创意页 → 保存并提交
- 人群流程：点定向radio → 查看/修改 → 自定义人群标签 → 勾 9 项 → 保存设置 → 确认抽屉关闭（**不点保存并提交**，由主流程下一步处理）
- 失败仅 WARN 不阻塞；`--dry-run` 同样执行

### 本次修复的脚本问题（batch_target_audience.py）

| 问题 | 修复 |
|------|------|
| 「保存设置」JS 点击可能未生效 → 提交失败 | 改真实鼠标点击 + 重试 3 次 + 确认抽屉关闭 |
| 「继续提交」确认弹窗未点（右侧抽屉） | 轮询 + 真实点击「继续提交/继续保存/继续」 |
| 提交成功无客观校验 | 以「提交成功」弹窗/离开 cpm-edit 为成功标志，否则记录失败 |
| 每条卡 2-3 分钟 | 所有 Playwright 操作加短超时（scroll 3s / click 5s），_goto_edit 当前页复用 |
| 100条/页设置无效 | 选项文本带空格（"100 条/页"），改去空格匹配 |
| 兴趣标签懒加载找不到 | TreeWalker 精确文本匹配 + 慢滚动（5步×0.4s）触发虚拟渲染，仍失败记录跳过 |

---

## 一、已解决的问题

### 核心流程修复（前期）

| # | 问题 | 修复 |
|---|------|------|
| 1 | 缓存续跑导致 SPA 状态错乱、前后对不上 | 删除整套缓存/续跑逻辑，每次完整重跑 |
| 2 | 门店下拉关闭方式触发表单重置（目的→门店引流、门店→默认） | 改点面包屑提交选择，不用鼠标点空白 |
| 3 | 笔记卡片不渲染 | 等 cpm-edit frame 加载 + 超时自动叉掉门店重选（最多 3 轮） |
| 4 | 推广地域/出价无法设置 | 回填选择器：门店附近区域 6km（drawer 流程）、单次点击出价 1.1 |
| 5 | 门店写死，每次改代码效率低 | `config/stores.json` 门店列表（37 家），自动化依次遍历 |
| 6 | 预算行在第 2 页找不到（只查第 1 页） | 逐页翻查（最多 6 页） |
| 7 | 门店下拉匹配失败（中文/英文括号差异） | 品牌词+分店词双关键词枚举匹配 |

### 今日新发现的重大 bug（均已修复并验证）

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 8 | **3 条推广都是同一篇「新手基础」** | `_scrape_notes` 把标题截断到 80 字符，`_check_note` 用完整标题比较 → 永远匹配失败 → 静默提交抽屉默认选中的笔记；查重库 mark 的标题和实际提交的笔记完全不是一回事 | 统一标题处理（normalize）；`_check_note` 优先 note_id 精确匹配、其次标题精确匹配；**匹配失败直接抛错终止，禁止静默提交** |
| 9 | 提交后弹窗（`vertical-top-dialog` 类型）没关掉，挡住「下一步」按钮 | 弹窗类型不固定，旧处理只覆盖 `.merchant-modal` 等部分类型 | 通用弹窗清理 `_dismiss_modals()`：匹配所有可见 modal/dialog，逐个点确定/确认/知道了/关闭，兜底 ESC；在「确认修改后/点下一步前/提交后」三处调用 |
| 10 | **400+ 浏览量的笔记被误判跳过**（化身福尔摩斯等） | `note_id` 无真实 data-id 时 fallback 为 `card-{i}`（抽屉位置序号），**跨门店重复** → 按 note_id 查重时与 Xcape 店已提交记录撞号 → 误判"已推广" | `notes.py`：仅真实 data-id 用于查重（`_is_real_note_id()`）；`flow.py`：`note_id = data-id or None`；**清理数据库中 28 条 card-N 假 ID**（title+store 精确查重不受影响） |
| 11 | 历史推广内容按「主题关键词」模糊过滤，误伤同主题不同笔记 | 共享预算的推广内容（如「猛诡禁区」）与候选笔记标题做子串匹配，主题相同就被过滤 | 改为**精确同标题匹配**（`_same_note()`：去掉"门店优质笔记"前缀后比较前 30 字） |

---

## 二、当前进度

```
✅ Xcape异时刻徐家汇店   24 条   （浏览量 ≥400 的笔记已全部推完，剩余最高 245 < 400 自动跳过）
🔄 鬼十八长宁来福士店   9 条   （修复后本次推了 5 条：7400 / 1434 / 1101 / 1002 / 756 浏览，尚未跑完）
⬜ 其余 35 家门店        未跑
```

**查重库**：33 条记录（全部今天提交），0 条 card-N 脏数据。

---

## 三、还有哪些问题

1. **鬼十八长宁来福士店未跑完** —— 剩余浏览量 ≥400 的笔记（化身福尔摩斯、今天在家查抓疯等）待推广
2. **美团后台遗留重复推广** —— 修复前 bug 造成的 3 条「新手基础」重复推广，需在美团后台手动删除（自动化无权删除后台推广）
3. **导航稳定性** —— 每轮导航可能失败 1-2 次（重试能恢复，但单轮耗时 2-3 分钟偏慢）
4. **全量跑 35 家门店** —— 每家 30-50 条 × 2-3 分钟/条 ≈ 每家 1.5-2.5 小时，35 家累计数十小时，**必须分批执行**
5. **每日上限** —— `max_promotions_per_day=100` 对单家门店够用，全量跑时需确认是否调大/取消
6. **Edge 调试模式不能关闭** —— 浏览器被关会导致任务中断（登录态在 profile 中，重启后需重新验证）

---

## 四、解决方案建议

| 事项 | 建议 |
|------|------|
| 长宁店剩余笔记 | 代码已就绪，下次执行直接 `--run --store "鬼十八恐怖密室逃脱·真人NPC·沉浸解压(长宁来福士店)"` 跑完 |
| 后台遗留重复推广 | 用户在美团后台手动删除（3 条「新手基础」重复推广） |
| 全量 35 家门店 | 按门店分批跑，每批 5-8 家，跑完汇报；或一次跑完全部（耗时数十小时） |
| 导航慢 | 可优化（减少每轮重置次数），但不阻塞主流程，优先级低 |
| 每日上限 | 全量跑前确认策略 |
| 浏览器 | 全程保持 Edge 打开 |

---

## 五、当前运行规则（代码已生效）

1. **门店来源**：`config/stores.json`（37 家，`enabled` 字段控制，无预算组关键词的自动跳过）
2. **去重规则**：
   - 查重库 `promoted_notes`：精确匹配 标题+门店（同一篇笔记只推一次）
   - 共享预算历史推广：**同标题**才算重复（精确匹配，不用主题关键词）
3. **浏览量门槛**：仅推广 **浏览量 ≥ 400** 的笔记；因按浏览量降序，遇第一篇 < 400 即停止该门店、跳下一家
4. **提交模式**：
   - `--run`：真实提交（保存并提交）+ 提交成功写入查重库
   - `--dry-run`：保存为草稿，不写库
5. **单门店上限**：该预算组「推广中」记录 ≥ 100 停止
